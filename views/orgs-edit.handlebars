<form method="POST" action="/orgs/{{charity._id}}?_method=PUT">

<legend>Edit Organization Info</legend>

{{> orgs-form}}

    <!-- BUTTON -->
    <p>
        <button type="submit">Save Organization</button>
    </p>
</form>